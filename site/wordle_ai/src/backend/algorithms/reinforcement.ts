import axios from "axios";
import LetterBoxProps from "../../interfaces/LetterBoxProps";
import getHost from "../getHost";

export function reinforcementGuess(target: string, board: LetterBoxProps[][]): LetterBoxProps[][] {
  return [];
}

export function reinforcementFull(target: string): LetterBoxProps[][] {
  return [];
}