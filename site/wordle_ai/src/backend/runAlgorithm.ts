import { Algorithm } from "../enums/Algorithm";
import LetterBoxProps from "../interfaces/LetterBoxProps";

export default function runAlgorithm(algorithm: Algorithm, target: string): LetterBoxProps[][] {
  return [];
}
